//Write a program to copy all elements of one array into another.
#include <stdio.h>

int main() {
    int arr1[100], arr2[100], n, i;

    printf("Enter size of array: ");
    scanf("%d", &n);

    printf("Enter %d elements: ", n);
    for (i = 0; i < n; i++) {
        scanf("%d", &arr1[i]);
    }

    for (i = 0; i < n; i++) {
        arr2[i] = arr1[i];
    }
    printf("Elements of second array: ");
    for (i = 0; i < n; i++) {
        printf("%d ", arr2[i]);
    }

    return 0;
}

output:

Enter size of array: 5
Enter 5 elements: 5 6 4 5 4
Elements of second array: 5 6 4 5 4 

=== Code Execution Successful ===
