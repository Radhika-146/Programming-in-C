//write a program to search a given number in an array.
#include <stdio.h>

int main() {
    int arr[] = {5,12,46,43,34,54,78,56,75,14,67,42,57,80,87};
    int num, found = 0;
    int n = sizeof(arr) / sizeof(arr[0]);

    printf("Enter the num to check: ");
    scanf("%d", &num);

    for (int i = 0; i < n; i++) {
        if (arr[i] == num)
        {
            found = 1;   
            break;       
        }
    }

    if (found)
        printf("found in array");   
    else
        printf("not found in array");

    return 0;
}

Enter the num to check: 45
not found in array

=== Code Execution Successful ===
